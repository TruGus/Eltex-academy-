#include <stdio.h>


#define N 5

int main() {
  int a[N];
  int b = 0;
  for(int i = 0; i <= 4; i++)
  {
    scanf("%d", &a[i]);
    
  }

  for(int i = 4; i >= 0; i--)
  {
    printf("%d", a[i]);
  }
  printf("\n");
  return 0;
}
