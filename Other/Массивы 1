#include <stdio.h>

#define M 3
#define N 3

int main() {
  int a[M][N];
  int b = 0;
  for(int i = 0; i <= 2; i++)
  {
      for(int j = 0; j<= 2; j++)
      {
        a[i][j] = b++;
        printf("%d ", a[i][j]);
      }

      printf("\n");
  }
  
  return 0;
}
